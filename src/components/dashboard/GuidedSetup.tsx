// This component is now replaced by FloatingGuide.tsx
// It is kept in the codebase to avoid breaking potential imports, but it does nothing.

import React from 'react';

export default function GuidedSetup() {
  return null;
}
