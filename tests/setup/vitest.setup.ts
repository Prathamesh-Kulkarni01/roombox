import '@testing-library/jest-dom';
import { beforeAll, afterAll, afterEach } from 'vitest';

// Global setup for Vitest
beforeAll(() => {
  // e.g. Start server or initialize mock Service Worker
});

afterEach(() => {
  // e.g. Cleanup after each test
});

afterAll(() => {
  // e.g. Teardown global resources
});
